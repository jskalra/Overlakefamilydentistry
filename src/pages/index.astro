---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import ReviewCarousel from "../components/ReviewCarousel.tsx";
import { serviceCategories } from "../data/site";

// ⚠️ PLACEHOLDER / ILLUSTRATIVE ONLY — do NOT launch as real reviews.
// Replace with real, permissioned Google reviews before go-live (FTC/LSA compliance).
const sampleReviews = [
  { name: "Illustrative example", years: "sample", text: "Three generations of our family see this team. They never push treatment we don't need — that trust is why we've stayed for years." },
  { name: "Illustrative example", years: "sample", text: "Gentle, honest, and thorough. They took a conservative approach when another office pushed bigger treatment." },
  { name: "Illustrative example", years: "sample", text: "New to Bellevue and found a dental home. The same friendly faces every visit — you feel remembered." },
];
---

<BaseLayout title="Overlake Family Dentistry — Conservative, Tooth-Preserving Care in Bellevue">
  <Hero />

  <!-- Services snapshot -->
  <section class="container-page section">
    <div class="text-center max-w-2xl mx-auto">
      <p class="eyebrow">Comprehensive In-House Care</p>
      <h2 class="text-3xl md:text-4xl font-semibold mt-2">Everything your family needs, under one roof</h2>
    </div>
    <div class="mt-10 grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {serviceCategories.map((s) => (
        <a href={s.href} class="card block">
          <div class="w-10 h-10 rounded-full bg-sage/20 mb-4"></div>
          <h3 class="text-xl font-semibold">{s.title}</h3>
          <p class="mt-2 text-slate text-sm">Learn more about our approach to {s.title.toLowerCase()}.</p>
        </a>
      ))}
    </div>
  </section>

  <!-- Reviews -->
  <section class="bg-cream section">
    <div class="container-page">
      <div class="text-center mb-10">
        <p class="eyebrow">Multi-Decade Patient Loyalty</p>
        <h2 class="text-3xl md:text-4xl font-semibold mt-2">Families who've trusted us for generations</h2>
      </div>
      <ReviewCarousel client:visible reviews={sampleReviews} />
      <p class="text-center mt-8 text-xs text-slate">Illustrative examples — real, verified Google reviews will be added before launch.</p>
    </div>
  </section>
</BaseLayout>
